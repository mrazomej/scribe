"""
Stage modules for the :func:`scribe.api.fit` orchestration pipeline.

Each module implements one stage of the ``fit`` pipeline, operating on
the shared :class:`~scribe.api.context.FitContext` state bag.
"""
