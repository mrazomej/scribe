---
editor:
    render-on-save: true
csl: ieee.csl
---

## Empirical CLR Differential Expression {#sec-diffexp-empirical}

The previous section fixed a compositional coordinate system for inference:
centered log-ratio (CLR) coordinates map simplex compositions to a Euclidean
space where gene-level contrasts are additive and interpretable. Once expression
is represented in CLR, the remaining question is how to summarize uncertainty
about differences between conditions without imposing a parametric shape on the
posterior predictive distribution.

We take a fully empirical route. The fitted model supplies posterior samples of
the Dirichlet concentration parameters; for each draw we simulate a composition
from the corresponding Dirichlet law, map it to CLR, and repeat independently
across posterior iterations. All differential expression summaries---means,
spreads, sign probabilities, practical-effect tail masses, and local false sign
rates---are obtained by counting over these Monte Carlo samples. No Gaussian or
other closed form is assumed for the predictive; the empirical histogram *is*
the inference object.

This empirical CLR pipeline is the **primary and recommended** differential
expression method in this chapter. It remains valid when the posterior
predictive is skewed, heavy-tailed, multimodal, or otherwise far from Gaussian,
because every quantity is defined as a posterior functional estimated by sample
averages and indicator counts.

What follows makes this construction precise: we define paired CLR difference
samples between conditions, derive gene-level statistics entirely from counts,
discuss Monte Carlo resolution and standard errors, show compatibility with
posterior expected false positive (PEFP) error control, note the option of
multiple Dirichlet draws per posterior sample, and summarize computational cost.
We then close with expression masking and its interaction with the CLR
reference, which must be handled before forming CLR coordinates so that
low-expression pooling does not distort sign probabilities.

#### From posterior samples to CLR differences

The starting point is the fitted Dirichlet-Multinomial model, which yields
posterior samples of the Dirichlet concentration parameters:

$$
\underline{r}^{(s)} \sim \pi(\underline{r} \mid \text{data}),
\quad s = 1, 2, \ldots, N.
$$ {#eq-diffexp-np-posterior-samples}

For each posterior sample, we draw from the Dirichlet predictive distribution to
obt ain a composition on the simplex:

$$
\underline{\rho}^{(s)} \sim \text{Dir}(\underline{r}^{(s)}),
\quad s = 1, 2, \ldots, N,
$$ {#eq-diffexp-np-dirichlet-draw}

where $\underline{\rho}^{(s)} = (\rho_1^{(s)}, \rho_2^{(s)}, \ldots,
\rho_D^{(s)})$ lies on the $(D{-}1)$-simplex. This two-step sampling---posterior
draw followed by predictive draw---correctly marginalizes over parameter
uncertainty.

We then apply the centered log-ratio transformation to map from the simplex to
an unconstrained space:

$$
\underline{z}_{\text{CLR}}^{(s)} = \text{CLR}(\underline{\rho}^{(s)}) =
\log \underline{\rho}^{(s)} -
\frac{1}{D}\sum_{g=1}^{D} \log \rho_g^{(s)} \cdot \underline{1},
$$ {#eq-diffexp-np-clr-transform}

where $\underline{1}$ is the vector of ones. The CLR coordinates sum to zero by
construction.

For two conditions A and B with posterior samples $\underline{r}_A^{(s)}$ and
$\underline{r}_B^{(s)}$, we form the CLR differences gene by gene:

$$
\Delta_g^{(s)} = z_{A,g,\text{CLR}}^{(s)} - z_{B,g,\text{CLR}}^{(s)},
\quad g = 1, \ldots, D, \quad s = 1, \ldots, N.
$$ {#eq-diffexp-np-delta-samples}

The matrix $\underline{\underline{\Delta}} \in \mathbb{R}^{N \times D}$ of CLR
differences constitutes the empirical posterior of the gene-level effect. All
subsequent DE statistics are computed from this matrix by counting.

#### Validity of sample pairing

A critical question is whether the index-pairing used in
@eq-diffexp-np-delta-samples---matching sample $s$ from condition A with sample
$s$ from condition B---is valid. We now establish this rigorously for both the
independent-model and within-mixture settings.

**Case 1: Independent models.** When conditions A and B have been fitted with
independent models on independent data, the joint posterior factorizes:

$$
\pi(\underline{r}_A, \underline{r}_B \mid \text{data}_A, \text{data}_B)
= \pi(\underline{r}_A \mid \text{data}_A)
\cdot \pi(\underline{r}_B \mid \text{data}_B).
$$ {#eq-diffexp-np-posterior-independence}

Let $f(\cdot)$ denote the composition of Dirichlet sampling and CLR
transformation: $f(\underline{r}) = \text{CLR}(\text{Dir}(\underline{r}))$. The
posterior of the difference $\Delta_g$ is defined by the induced measure:

$$
\pi(\Delta_g \mid \text{data}_A, \text{data}_B) =
\int \delta\!\left(\Delta_g - [f(\underline{r}_A) -
f(\underline{r}_B)]_g\right) \,
\pi(\underline{r}_A \mid \text{data}_A) \,
\pi(\underline{r}_B \mid \text{data}_B) \,
d\underline{r}_A \, d\underline{r}_B,
$$ {#eq-diffexp-np-delta-posterior}

where $\delta(\cdot)$ is the Dirac delta. By
@eq-diffexp-np-posterior-independence, this integral is over the product measure
$\pi(\underline{r}_A \mid \text{data}_A) \otimes \pi(\underline{r}_B \mid
\text{data}_B)$.

Now, suppose we have $N$ i.i.d. samples from each marginal posterior:
$\{\underline{r}_A^{(s)}\}_{s=1}^{N}$ and $\{\underline{r}_B^{(s)}\}_{s=1}^{N}$.
For any permutation $\sigma$ of $\{1, \ldots, N\}$, the pairs
$\{(\underline{r}_A^{(s)}, \underline{r}_B^{(\sigma(s))})\}_{s=1}^{N}$ are
i.i.d. draws from the product measure, because $\underline{r}_A^{(s)}$ and
$\underline{r}_B^{(\sigma(s))}$ are independent regardless of $\sigma$. By the
strong law of large numbers applied to the product measure, the empirical
distribution of

$$
\tilde{\Delta}_g^{(s)} = [f(\underline{r}_A^{(s)}) -
f(\underline{r}_B^{(\sigma(s))})]_g, \quad s = 1, \ldots, N,
$$ {#eq-diffexp-np-permuted-delta}

converges almost surely to $\pi(\Delta_g \mid \text{data}_A, \text{data}_B)$ as
$N \to \infty$, for any fixed permutation $\sigma$.

In particular, the identity permutation $\sigma(s) = s$ is a valid choice.
Index-pairing is therefore an arbitrary but correct pairing strategy for
independent models. $\square$

**Case 2: Within-mixture comparisons.** When both conditions are components of a
single mixture model, the posterior samples are drawn jointly:

$$
(\underline{r}_1^{(s)}, \underline{r}_2^{(s)}, \ldots,
\underline{r}_K^{(s)}) \sim
\pi(\underline{r}_1, \ldots, \underline{r}_K \mid \text{data}),
$$ {#eq-diffexp-np-joint-posterior}

where $K$ is the number of mixture components. In this setting, the parameters
for different components within the same posterior draw are generally **not
independent**: they share information through the likelihood (all components
explain the same observed cells) and through the prior.

For comparing components $k$ and $k'$, we must preserve the sample-index
pairing:

$$
\Delta_g^{(s)} = z_{k,g,\text{CLR}}^{(s)} - z_{k',g,\text{CLR}}^{(s)},
$$ {#eq-diffexp-np-within-mixture-delta}

because the joint posterior structure between components $k$ and $k'$ is only
correctly represented when both are drawn from the same posterior sample $s$.
Permuting the samples would destroy the correlation structure and produce
incorrect posterior estimates of the difference.

In our implementation, this is enforced by the `paired=True` flag, which uses
the same per-sample RNG sub-key for both components when drawing from the
Dirichlet predictive, ensuring that the joint structure is preserved throughout
the sampling pipeline.

#### Empirical gene-level statistics

Given $N$ samples $\Delta_g^{(1)}, \Delta_g^{(2)}, \ldots, \Delta_g^{(N)}$ of
the CLR difference for gene $g$, we compute all DE statistics by direct
counting. No distributional assumption is required.

**Posterior mean.** The posterior mean of the CLR difference is estimated by the
sample mean:

$$
\hat{\mu}_{\Delta,g} = \frac{1}{N}\sum_{s=1}^{N} \Delta_g^{(s)}.
$$ {#eq-diffexp-np-mean}

**Posterior standard deviation.** The posterior standard deviation is estimated
by the sample standard deviation with Bessel's correction:

$$
\hat{\sigma}_{\Delta,g} = \sqrt{
\frac{1}{N-1}\sum_{s=1}^{N}
\left(\Delta_g^{(s)} - \hat{\mu}_{\Delta,g}\right)^2
}.
$$ {#eq-diffexp-np-sd}

**Probability of positive effect.** The posterior probability that gene $g$ is
up-regulated in condition A relative to B is estimated by the empirical
frequency:

$$
\hat{P}(\Delta_g > 0 \mid \text{data}) = \frac{1}{N}\sum_{s=1}^{N}
\mathbf{1}\!\left[\Delta_g^{(s)} > 0\right].
$$ {#eq-diffexp-np-prob-positive}

**Empirical local false sign rate.** The lfsr, defined in
@eq-diffexp-lfsr-definition as the posterior probability of the minority sign,
is estimated by

$$
\widehat{\text{lfsr}}_g = \min\!\left(
\hat{P}(\Delta_g > 0 \mid \text{data}), \;
1 - \hat{P}(\Delta_g > 0 \mid \text{data})
\right).
$$ {#eq-diffexp-np-lfsr}

This is precisely the smaller of the empirical probabilities of positive and
negative effects. Note that no Gaussian CDF evaluation is involved.

**Probability of practical significance.** For a practical significance
threshold $\tau \geq 0$, the probabilities of upward and downward practical
effects are

$$
\hat{P}(\Delta_g > \tau \mid \text{data}) = \frac{1}{N}\sum_{s=1}^{N}
\mathbf{1}\!\left[\Delta_g^{(s)} > \tau\right],
$$ {#eq-diffexp-np-prob-above-tau}

$$
\hat{P}(\Delta_g < -\tau \mid \text{data}) = \frac{1}{N}\sum_{s=1}^{N}
\mathbf{1}\!\left[\Delta_g^{(s)} < -\tau\right],
$$ {#eq-diffexp-np-prob-below-tau}

and the total probability of practical significance is

$$
\hat{P}(|\Delta_g| > \tau \mid \text{data}) =
\hat{P}(\Delta_g > \tau \mid \text{data}) +
\hat{P}(\Delta_g < -\tau \mid \text{data}).
$$ {#eq-diffexp-np-prob-effect}

**Empirical lfsr with practical significance.** Analogous to the parametric
$\text{lfsr}_g(\tau)$ defined in @eq-diffexp-lfsr-tau, the empirical
practical-significance lfsr is

$$
\widehat{\text{lfsr}}_g(\tau) = 1 - \max\!\left(
\hat{P}(\Delta_g > \tau \mid \text{data}), \;
\hat{P}(\Delta_g < -\tau \mid \text{data})
\right).
$$ {#eq-diffexp-np-lfsr-tau}

When $\tau = 0$, this reduces to the standard empirical lfsr
(@eq-diffexp-np-lfsr), since $\max(P(\Delta_g > 0), P(\Delta_g < 0)) = 1 -
\text{lfsr}_g$.

**Consistency with the parametric framework.** The empirical lfsr
$\widehat{\text{lfsr}}_g$ estimates the same quantity as the parametric lfsr
$\text{lfsr}_g$---namely, the posterior probability of the minority sign. When
the Gaussian assumption holds, the two estimates converge to the same value as
$N \to \infty$. When it does not hold, the empirical estimate is correct while
the parametric estimate may be biased.

#### Monte Carlo resolution and standard errors

The empirical lfsr estimator $\widehat{\text{lfsr}}_g$ is a sample proportion.
Its precision is governed by the binomial standard error.

**Standard error of the empirical lfsr.** Define $p_g = P(\Delta_g > 0 \mid
\text{data})$ (the true posterior probability of positive effect). The empirical
estimator $\hat{p}_g = \frac{1}{N}\sum_s \mathbf{1}[\Delta_g^{(s)}
> 0]$ has variance $\text{Var}(\hat{p}_g) = p_g(1 - p_g)/N$. The lfsr is $\ell_g
> = \min(p_g, 1 - p_g)$, and its estimator $\hat{\ell}_g = \min(\hat{p}_g, 1 -
> \hat{p}_g)$. By the delta method (since $\ell_g$ is a differentiable function
> of $p_g$ away from $p_g = 1/2$), the standard error of $\hat{\ell}_g$ is

$$
\text{SE}(\widehat{\text{lfsr}}_g) \approx
\sqrt{\frac{\ell_g (1 - \ell_g)}{N}}.
$$ {#eq-diffexp-np-lfsr-se}

This expression is exact for $p_g \neq 1/2$ and provides an upper bound at $p_g
= 1/2$ (where $\ell_g = 1/2$ and $\text{SE} = 1/(2\sqrt{N})$).

**Numerical examples.** For $N = 10{,}000$ posterior samples:

| True lfsr | SE of empirical lfsr | Relative SE |
|-----------|----------------------|-------------|
| 0.50      | 0.005                | 1.0%        |
| 0.10      | 0.003                | 3.0%        |
| 0.05      | 0.0022               | 4.3%        |
| 0.01      | 0.001                | 10%         |
| 0.001     | 0.0003               | 32%         |

For most practical purposes, $N = 10{,}000$ provides sufficient resolution to
distinguish truly significant genes (lfsr $\lesssim 0.01$) from non-significant
ones. The standard error for lfsr $= 0.01$ is approximately 0.001, which is well
below the typical PEFP threshold of $\alpha = 0.05$.

**Minimum detectable lfsr.** The smallest non-zero empirical lfsr is $1/N$,
achieved when all $N$ samples agree on the sign. For $N = 10{,}000$, this gives
a resolution floor of $10^{-4}$. Genes with true lfsr smaller than $1/N$ cannot
be distinguished from lfsr $= 0$ by the empirical method. In contrast, the
parametric lfsr can represent arbitrarily small values via the Gaussian CDF.
This resolution difference is the fundamental tradeoff between the parametric
and empirical approaches.

**Confidence interval.** Using the normal approximation to the binomial, an
approximate $(1-\alpha)$ confidence interval for the true lfsr is

$$
\widehat{\text{lfsr}}_g \pm z_{\alpha/2}
\sqrt{\frac{\widehat{\text{lfsr}}_g
(1 - \widehat{\text{lfsr}}_g)}{N}},
$$ {#eq-diffexp-np-lfsr-ci}

where $z_{\alpha/2}$ is the $(1 - \alpha/2)$-quantile of the standard normal.

#### Compatibility with PEFP error control

A key design property of the empirical DE approach is that the lfsr values it
produces are **fully compatible** with the PEFP error control machinery
developed in @sec-diffexp-pefp. The PEFP (@eq-diffexp-pefp-lfsr) is defined as

$$
\text{PEFP}(S) = \frac{1}{|S|} \sum_{g \in S} \text{lfsr}_g,
$$ {#eq-diffexp-np-pefp}

which depends only on the numerical lfsr values, not on how they were computed.
The threshold-finding algorithm (@eq-diffexp-algorithm-step1 through
@eq-diffexp-algorithm-step4) and the monotonicity proof
(@eq-diffexp-pefp-monotone) are purely algebraic properties of the sorted lfsr
vector and apply identically to empirical lfsr values.

Therefore, the same `call_de_genes`, `compute_pefp`, and `find_lfsr_threshold`
functions can be used with either parametric or empirical lfsr values, with no
modification.

#### Multiple Dirichlet draws per posterior sample

In certain settings, one may wish to draw multiple compositions from the
Dirichlet predictive for each posterior sample of $\underline{r}$. If we draw
$M$ compositions per posterior sample, the total number of CLR difference
samples becomes $N_{\text{total}} = N \times M$, and all the statistics in this
section are computed over the expanded sample.

This strategy can improve the resolution of the empirical lfsr without requiring
additional (computationally expensive) posterior samples. However, the $M$
Dirichlet draws from a single posterior sample $\underline{r}^{(s)}$ are
conditionally independent given $\underline{r}^{(s)}$ but not marginally
independent; they share the same parameter value. As a result, the effective
sample size for posterior uncertainty quantification remains $N$ (the number of
distinct posterior samples), even though the nominal sample count is $NM$.

The primary benefit of $M > 1$ is in resolving the Dirichlet sampling noise
(which is distinct from posterior uncertainty). For most applications with $N
\geq 1{,}000$, $M = 1$ provides sufficient resolution, and we recommend this as
the default.

#### Computational complexity

The computational cost of the empirical DE pipeline consists of three stages:

**Stage 1: Dirichlet sampling** ($O(ND)$ per batch). For each posterior sample
$\underline{r}^{(s)} \in \mathbb{R}^D$, drawing from
$\text{Dir}(\underline{r}^{(s)})$ requires $D$ gamma variates followed by
normalization. With batched sampling (processing $B$ samples simultaneously),
the memory footprint is $O(BD)$ and the total work is $O(ND)$.

**Stage 2: CLR transformation** ($O(ND)$). Applying the CLR transform to the $N
\times D$ matrix of simplex samples requires computing the geometric mean (one
reduction per row) and subtracting it.

**Stage 3: Counting statistics** ($O(ND)$). Computing means, standard
deviations, and indicator sums over $N$ samples for $D$ genes.

| **Stage**           | **Complexity** | **Memory** | **GPU-parallelizable** |
|---------------------|----------------|------------|------------------------|
| Dirichlet sampling  | $O(ND)$        | $O(BD)$    | Yes (batched)          |
| CLR transformation  | $O(ND)$        | $O(ND)$    | Yes                    |
| Counting statistics | $O(ND)$        | $O(D)$     | Yes                    |
| **Total**           | $O(ND)$        | $O(BD)$    | Fully                  |

where $B$ is the batch size for Dirichlet sampling (default: 2048).

**Comparison to the parametric path.** The parametric DE requires fitting the
logistic-normal approximation ($O(ND)$ for the SVD-based low-rank fit, done
once) and then evaluates per-gene statistics in $O(kD)$ time, where $k$ is the
low-rank dimension. The empirical path is $O(ND)$ throughout. For typical values
of $N = 10{,}000$, $D = 20{,}000$, and $k = 32$:

| **Method** | **One-time cost** | **Per-gene cost** | **Total for** $D$ genes | **Assumptions** |
|--------------|--------------|--------------|----------------|--------------|
| Parametric | $O(NDk)$ SVD fit | $O(k)$ | $O(kD) \approx 6 \times 10^5$ | Gaussian marginals |
| Empirical | $O(ND)$ sampling | $O(N)$ | $O(ND) \approx 2 \times 10^8$ | None |

The empirical approach is approximately $N/k \approx 300\times$ more expensive
per analysis, but requires no distributional assumptions. On modern GPU
hardware, the empirical analysis for $D = 20{,}000$ genes with $N = 10{,}000$
samples completes in seconds, making the additional cost negligible in practice.

### Expression masking and the CLR reference composition {#sec-diffexp-expression-mask}

An expression mask filters low-expression genes before differential analysis,
pooling them into a single "other" pseudo-gene to maintain compositional
closure. Because the centered log-ratio (CLR) transform uses the geometric mean
of all retained genes as its reference, the choice of mask directly defines the
yardstick against which effects are measured. This section formalises the
consequences.

**Setup.** Let $\boldsymbol\rho^{(s)}$ denote a simplex sample of dimension $D$,
partitioned into kept genes $\mathcal{K}$ ($|\mathcal{K}| = D_k$) and filtered
genes $\mathcal{F}$. Write $\rho_{\mathrm{other}}^{(s)} =
\sum_{g \in \mathcal{F}} \rho_g^{(s)}$ for the aggregated "other" proportion.
The masked simplex has $D_k + 1$ components.

**CLR under the mask.** The masked CLR for a kept gene $g \in \mathcal{K}$ is:

$$
\mathrm{CLR}_g^{\mathrm{mask},(s)} = \log \rho_g^{(s)} - \frac{1}{D_k + 1}
\left(\sum_{j \in \mathcal{K}} \log \rho_j^{(s)} +
\log \rho_{\mathrm{other}}^{(s)}\right),
$$

whereas the full-gene CLR would use all $D$ individual log-proportions in the
geometric mean. For each posterior sample, the difference between the full-gene
CLR and the masked CLR for a kept gene is a *per-sample constant*:

$$
c^{(s)} = \mathrm{CLR}_g^{\mathrm{full},(s)} -
\mathrm{CLR}_g^{\mathrm{mask},(s)} =
\log \bar{g}_{\mathrm{mask}}^{(s)} -
\log \bar{g}_{\mathrm{full}}^{(s)},
$$

where $\bar{g}_{\mathrm{mask}}^{(s)}$ and $\bar{g}_{\mathrm{full}}^{(s)}$ are
the geometric means of the masked and full compositions, respectively. This
shift $c^{(s)}$ is the same for every gene $g \in \mathcal{K}$ in a given
sample.

**Why the shift matters.** Although $\text{Var}[c^{(s)}]$ is typically $O(1/D)$
(the shift is tight across samples), its expectation $\langle c^{(s)} \rangle$
can be large. In log-space, low-expression genes dominate the geometric mean
because their $\log \rho_g$ values are large negative numbers. When many genes
are low-expression (common in transcriptomics), the full-gene geometric mean is
pulled strongly downward relative to the masked geometric mean that only
includes expressed genes. This produces a substantial constant offset in
$c^{(s)}$ that shifts all $\Delta_g^{(s)}$ uniformly.

Although the shift is constant across genes within each sample, the magnitude
and sign of the shift vary across posterior samples, so it does not cancel when
averaging. The resulting bias completely scrambles the local false sign rate
(lfsr): genes that should have lfsr $\approx 0$ acquire spurious probability
mass on the wrong sign, and vice versa. **Therefore, the expression mask must be
applied before the CLR transform, not as a post-hoc filter on the CLR
differences.**

**Dirichlet aggregation property.** Fortunately, the Dirichlet distribution has
a closure property under aggregation: if $\boldsymbol\rho \sim
\mathrm{Dir}(\boldsymbol\alpha)$, then the composition formed by summing any
subset of components into a single entry is itself Dirichlet with the summed
concentration. Formally, if we aggregate $\mathcal{F}$ into "other":

$$
(\rho_{\mathcal{K}_1}, \ldots, \rho_{\mathcal{K}_{D_k}},
\rho_{\mathrm{other}}) \sim
\mathrm{Dir}(\alpha_{\mathcal{K}_1}, \ldots, \alpha_{\mathcal{K}_{D_k}},
\alpha_{\mathrm{other}}),
\quad
\alpha_{\mathrm{other}} = \sum_{g \in \mathcal{F}} \alpha_g.
$$

This means we can store the full $D$-dimensional simplex samples from the
initial Dirichlet draw and re-aggregate post-hoc for any gene mask—the result is
*mathematically identical* to having pre-aggregated the concentration parameters
and sampled the lower-dimensional Dirichlet. This enables interactive mask
exploration on a single results object without re-running the expensive
Dirichlet/Gamma sampling stage.

```{=html}
<!-- TODO: Add diagnostic figure (full-gene vs masked CLR scatter for delta_mean,
     delta_sd, lfsr, and histogram of the constant shift) using a public dataset.
     See altos/exploratory/panfibrosis/panfibrosis_de_eda.py for the template. -->
```

### Mixture-Weighted Differential Expression for Heterogeneous Cell Types {#sec-diffexp-mixture-weighted}

The empirical CLR pipeline described above produces compositional differences
for a single fitted model per condition. In practice, however, a cell type may
harbour internal heterogeneity that a single Dirichlet-Multinomial component
cannot capture. A mixture model with $K$ components can represent distinct
transcriptional states within what the analyst considers a single cell
type---for example, quiescent and activated fibroblasts, or cycling and
non-cycling progenitors. The question then becomes: **how does the cell type *as
a whole* change between experimental conditions?**

The per-component DE developed earlier answers a narrower question---"how does
state $k$ change?"---by slicing the posterior along the component axis and
running the standard pipeline on a single component. This is appropriate when
the analyst has identified biologically meaningful components and wants to
compare them individually. But when the mixture is used purely as a flexible
density estimator to accommodate within-cell-type variability, a
**population-level** comparison that marginalises over the mixture is more
natural.

We now extend the empirical CLR pipeline to this setting. The key idea is
simple: rather than sampling a composition from a single component, we sample
compositions from *all* $K$ components and form a weighted average on the
simplex using the posterior mixture weights. The resulting weighted composition
feeds into the same CLR transformation and differencing machinery developed
above, so all downstream statistics---lfsr, PEFP, pathway tests---apply without
modification.

#### The marginal composition of a mixture model

Consider a $K$-component mixture model for a single cell type in one
experimental condition. The model assigns each cell to component $k$ with
probability $\pi_k$, where the mixture weights satisfy the simplex constraint
$\pi_k \geq 0$ and $\sum_{k=1}^{K} \pi_k = 1$. Conditional on component $k$, the
cell's normalised expression vector lies on the $D$-simplex:

$$
\underline{\rho} \mid k \sim \text{Dir}(\underline{r}_k),
$$ {#eq-mixture-component-dirichlet}

where $\underline{r}_k = (r_{1,k}, r_{2,k}, \ldots, r_{D,k})$ are the
component-specific Dirichlet concentration parameters.

The marginal distribution of the composition for a randomly sampled cell from
this cell type is the *mixture of Dirichlets*:

$$
p(\underline{\rho}) = \sum_{k=1}^{K} \pi_k \, \text{Dir}(\underline{\rho}
\mid \underline{r}_k).
$$ {#eq-mixture-of-dirichlets}

This is not itself a Dirichlet distribution---it can be multimodal and have a
richer covariance structure than any single Dirichlet. The marginal expected
composition of gene $g$ is

$$
\langle \rho_g \rangle = \sum_{k=1}^{K} \pi_k \,
\frac{r_{g,k}}{r_{T,k}},
$$ {#eq-mixture-expected-composition}

where $r_{T,k} = \sum_{g=1}^{D} r_{g,k}$ is the total concentration for
component $k$. This is a weighted average of the *per-component mean
proportions*, not the proportion derived from a weighted average of the
concentration parameters.

#### Why aggregation must happen on the simplex, not at the $r$ level

A natural but incorrect shortcut would be to average the concentration
parameters across components,

$$
\bar{r}_g = \sum_{k=1}^{K} \pi_k \, r_{g,k},
$$ {#eq-r-averaging}

and then sample a single Dirichlet $\underline{\rho} \sim
\text{Dir}(\bar{\underline{r}})$. This fails for two reasons.

**Wrong mean.** The mean of $\text{Dir}(\bar{\underline{r}})$ is

$$
\frac{\bar{r}_g}{\bar{r}_T} =
\frac{\sum_{k=1}^{K} \pi_k \, r_{g,k}}{\sum_{k=1}^{K} \pi_k \, r_{T,k}},
$$ {#eq-r-averaging-mean}

which is in general *not* equal to the correct marginal mean in
@eq-mixture-expected-composition unless all components have the same total
concentration $r_{T,k} = r_T$. The discrepancy arises because normalisation does
not commute with weighted sums: components with larger total concentration are
implicitly upweighted in the $r$-averaged Dirichlet, biasing the result.

To see this concretely, consider two components ($K=2$) with equal mixture
weights $\pi_1 = \pi_2 = \frac{1}{2}$ and two genes ($D=2$):

$$
\underline{r}_1 = (1, 9), \quad \underline{r}_2 = (9, 1).
$$

The correct marginal mean for gene 1 is $\frac{1}{2} \cdot \frac{1}{10} +
\frac{1}{2} \cdot \frac{9}{10} = 0.5$. But the $r$-averaged concentration is
$\bar{\underline{r}} = (5, 5)$, whose Dirichlet mean is $\frac{5}{10} = 0.5$.
Here the means happen to agree because both total concentrations are 10. Now
change the second component to $\underline{r}_2 = (90, 10)$, so $r_{T,2} = 100$.
The correct mean is $\frac{1}{2} \cdot \frac{1}{10} + \frac{1}{2} \cdot
\frac{90}{100} = 0.5$, but the $r$-averaged mean is $\frac{45.5}{55} \approx
0.827$---a substantial bias toward the high-concentration component.

**Wrong variance.** Even when the means coincidentally agree, the variance of
$\text{Dir}(\bar{\underline{r}})$ is that of a *single* Dirichlet with
intermediate concentration. It cannot represent the multimodal or heavy-tailed
structure of the true mixture. By the law of total variance, the true marginal
variance decomposes as

$$
\text{Var}_{\text{mix}}(\rho_g) =
\underbrace{\sum_{k=1}^{K} \pi_k \, \text{Var}_k(\rho_g)}_{\text{within-component}}
\;+\;
\underbrace{\sum_{k=1}^{K} \pi_k \left(\mu_{g,k} - \bar{\mu}_g\right)^2}_{\text{between-component}},
$$ {#eq-mixture-variance-decomposition}

where $\mu_{g,k} = r_{g,k} / r_{T,k}$. The between-component term is necessarily
non-negative and is lost entirely by collapsing to a single Dirichlet.

The correct approach is therefore to sample compositions from each component
*separately*, then combine them on the simplex using the mixture weights.

#### The mixture-weighted CLR pipeline

Given posterior samples $\underline{r}_k^{(s)}$ for each component $k = 1,
\ldots, K$ and posterior samples of the mixture weights $\underline{\pi}^{(s)} =
(\pi_1^{(s)}, \ldots, \pi_K^{(s)})$, the mixture-weighted CLR pipeline proceeds
as follows.

**Step 1: Sample per-component compositions.** For each posterior sample $s$ and
each component $k$, draw an independent composition from the Dirichlet
predictive:

$$
\underline{\rho}_k^{(s)} \sim \text{Dir}(\underline{r}_k^{(s)}),
\quad k = 1, \ldots, K.
$$ {#eq-mixture-per-component-draw}

**Step 2: Form the mixture-weighted composition.** Combine the per-component
compositions using the posterior mixture weights:

$$
\bar{\underline{\rho}}^{(s)} = \sum_{k=1}^{K} \pi_k^{(s)} \,
\underline{\rho}_k^{(s)}.
$$ {#eq-mixture-weighted-composition}

This is a convex combination of simplex vectors and is therefore itself on the
simplex, since

$$
\bar{\rho}_g^{(s)} = \sum_{k=1}^{K} \pi_k^{(s)} \, \rho_{g,k}^{(s)}
\geq 0
\quad \text{and} \quad
\sum_{g=1}^{D} \bar{\rho}_g^{(s)} = \sum_{k=1}^{K} \pi_k^{(s)}
\underbrace{\sum_{g=1}^{D} \rho_{g,k}^{(s)}}_{=\,1} = 1.
$$ {#eq-mixture-simplex-preservation}

**Step 3: CLR transform and difference.** Apply the centered log-ratio
transformation to the weighted compositions for both conditions A and B, then
form the paired difference:

$$
\Delta_g^{(s)} = \text{CLR}(\bar{\underline{\rho}}_A^{(s)})_g -
\text{CLR}(\bar{\underline{\rho}}_B^{(s)})_g.
$$ {#eq-mixture-clr-delta}

**Step 4: Compute statistics.** All gene-level DE statistics---posterior mean,
standard deviation, lfsr, lfsr$_\tau$, probability of practical effect---are
computed from the matrix $\{\Delta_g^{(s)}\}_{s,g}$ by the same counting
procedures described earlier in this section. No distributional assumptions are
required.

#### Propagation of mixture-weight uncertainty

A critical feature of this construction is that the mixture weights
$\underline{\pi}^{(s)}$ are *posterior samples*, not fixed point estimates. The
model's variational posterior (or MCMC chain) over the mixture weights is a
Dirichlet distribution whose concentration parameters are learned during
inference. Using posterior draws of $\underline{\pi}$ means that uncertainty
about the relative abundance of cellular states is automatically propagated into
the DE statistics.

If the data strongly constrain the mixture proportions (e.g., 90\% quiescent
with tight posterior), the mixture weights will be nearly constant across
samples and the DE statistics will be dominated by within-state expression
differences. If the proportions are uncertain (e.g., similar proportions with
wide posterior), the additional variability in $\underline{\pi}^{(s)}$ inflates
the standard deviation of $\Delta_g^{(s)}$, appropriately increasing the lfsr
and reducing the number of called DE genes.

This stands in contrast to using a fixed point estimate $\hat{\pi}_k$ for the
mixture weights, which would understate the true posterior uncertainty about the
cell type's overall expression profile.

#### Invariance to capture probability

The thinning-invariance argument from @sec-diffexp-robustness extends directly
to the mixture-weighted pipeline. Recall that the key property is that the NB
shape parameter $r_g$ is invariant to binomial thinning, so the Dirichlet
concentrations $\underline{r}_k$ do not depend on the capture probability. Since
the mixture weights $\pi_k$ represent the probability of a cell belonging to
state $k$---a biological quantity that does not depend on the capture efficiency
of the sequencing protocol---the weighted composition
$\bar{\underline{\rho}}^{(s)}$ inherits the capture-probability invariance from
its components.

#### Reduction to existing pipelines

The mixture-weighted pipeline is a strict generalisation of the standard
empirical pipeline:

- **$K = 1$**: The only component receives weight $\pi_1 = 1$, and
  $\bar{\underline{\rho}}^{(s)} = \underline{\rho}_1^{(s)}$. This is exactly the
  standard pipeline from the beginning of this section.

- **Indicator weights**: If $\pi_k = \mathbb{1}[k = k^*]$ for some fixed
  component $k^*$, then $\bar{\underline{\rho}}^{(s)} =
  \underline{\rho}_{k^*}^{(s)}$. This is the per-component pipeline obtained by
  slicing a single mixture component.

Both limiting cases are recovered exactly, establishing backward compatibility.

#### Extension to biological-level metrics

The biological-level DE metrics developed in @sec-diffexp-biological---log-fold
change (LFC), log-variance ratio (LVR), and Gamma Jeffreys divergence---operate
directly on the negative binomial parameters $(r_g, p_g)$ rather than on the
simplex. For mixture-weighted biological DE, the natural extension is to form
the expected NB mean across components:

$$
\bar{\mu}_g^{(s)} = \sum_{k=1}^{K} \pi_k^{(s)} \, \mu_{g,k}^{(s)},
$$ {#eq-mixture-bio-mu}

where $\mu_{g,k}^{(s)} = r_{g,k}^{(s)} (1 - p_{g,k}^{(s)}) / p_{g,k}^{(s)}$ is
the NB mean for gene $g$ in component $k$. The weighted dispersion parameter is

$$
\bar{r}_g^{(s)} = \sum_{k=1}^{K} \pi_k^{(s)} \, r_{g,k}^{(s)},
$$ {#eq-mixture-bio-r}

and the effective success probability is derived from the weighted mean and
dispersion to maintain consistency with the NB parameterisation:

$$
\bar{p}_g^{(s)} = \frac{\bar{r}_g^{(s)}}{\bar{r}_g^{(s)} +
\bar{\mu}_g^{(s)}}.
$$ {#eq-mixture-bio-p}

These weighted parameters are then fed into the same LFC, LVR, and Gamma
Jeffreys divergence formulae, with all statistics computed by counting over
posterior samples as before.

#### Compatibility with expression masking

The expression mask described in @sec-diffexp-expression-mask applies to the
mixture-weighted pipeline without modification. The mask is applied to the
full-dimensional weighted simplex $\bar{\underline{\rho}}^{(s)}$ before CLR
transformation, exactly as in the standard pipeline. The Dirichlet aggregation
property ensures that genes pooled into the "other" pseudo-gene retain their
correct total proportion. Because the weighted composition is itself a valid
simplex vector, the mask operates on it identically to how it operates on a
single-component composition.

#### Computational cost

The mixture-weighted pipeline requires $K$ Dirichlet draws per posterior sample
per condition, compared to 1 in the standard pipeline. The weighted averaging
step is a single vectorised operation of cost $O(KND)$. For typical mixture
models with $K = 2$--$5$, this represents a modest constant-factor overhead on
the sampling stage, which is already JIT-compiled and GPU-accelerated. All
downstream operations---CLR transformation, differencing, gene-level statistics,
PEFP error control, and pathway analysis---remain $O(ND)$ regardless of $K$,
since they operate on the same $(N, D)$ matrix of CLR differences.